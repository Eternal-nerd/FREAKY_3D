#include "slider.h"

/*-----------------------------------------------------------------------------
------------------------------INITIALIZATION-----------------------------------
-----------------------------------------------------------------------------*/
void Slider::init(const std::string& id, OverlayMode mode, glm::vec2 position, glm::vec2 size, glm::vec2 extent, int knobTexIndex, int barTexIndex) {

    // init elements
    //knob_.init("knob", OVERLAY_MENU, { 0.5f, -0.5 }, { 200, 100 }, extent, { 0,0,1,1 }, 2);
    //bar_.init("bar", OVERLAY_MENU, { 0.5f, -0.3 }, { 200, 100 }, extent, { 0,0,1,1 }, 3);


}

